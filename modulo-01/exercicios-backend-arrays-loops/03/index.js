const numeros = [54, 22, 14, 87, 10, 284]

function findNumber(numeros) {
  let index = 0

  for (number of numeros) {
    if (number === 10) {
      break
    }

    index += 1
  }

  return index
}

console.log(findNumber(numeros))

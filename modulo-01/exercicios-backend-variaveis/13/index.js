function calcCylinderArea(radius, cylinderHeight) {
  const totalCylinderArea = 2 * 3 * ( 3 + 1)
  
  return totalCylinderArea
}

const radius = 3
const cylinderHeight = 1

console.log(calcCylinderArea(radius, cylinderHeight))